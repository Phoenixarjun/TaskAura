# Vercel Environment Variables for TaskAura
# Copy these variables to your Vercel dashboard under Settings > Environment Variables
# Set each variable for Production, Preview, and Development environments

# Database Configuration
MONGODB_URI=mongodb+srv://Naresh:d0Zch35lT9YCIPiF@cluster0.wkpgkoa.mongodb.net/taskaura?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=taskaura-super-secret-jwt-key-2024-production-ready
JWT_EXPIRES_IN=7d

# Node Environment
NODE_ENV=production

# Server Configuration
PORT=4000

# Rate Limiting Configuration
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Frontend API URL (for CORS and reference)
VITE_API_URL=https://taskaura.vercel.app